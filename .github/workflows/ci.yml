name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  # 用 go.mod 声明的最低版本跑构建与测试，保证声明的兼容性是真的。
  # 注意 -race 需要 cgo，所以这里不能设置 CGO_ENABLED=0。
  test:
    name: Test (Go from go.mod)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod

      - name: go mod verify / tidy check
        run: |
          go mod verify
          go mod tidy
          git diff --exit-code -- go.mod go.sum

      - name: gofmt
        run: |
          out="$(gofmt -l .)"
          if [ -n "$out" ]; then
            echo "以下文件未经 gofmt 格式化："; echo "$out"; exit 1
          fi

      - name: go vet (all target OSes)
        run: |
          go vet ./...
          GOOS=windows go vet ./...
          GOOS=darwin go vet ./...

      - name: go test
        run: go test -race -count=1 ./...

  # staticcheck / govulncheck 的最新版通常要求最新的 Go，而 setup-go 会设置
  # GOTOOLCHAIN=local 禁止自动下载工具链——所以这两个工具单独用当前稳定版 Go 运行。
  lint:
    name: Lint (Go stable)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version: stable

      - name: staticcheck
        uses: dominikh/staticcheck-action@v1
        with:
          version: latest
          install-go: false

      - name: govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...

  build:
    name: Build ${{ matrix.os }}/${{ matrix.arch }}
    needs: [test, lint]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: linux,   arch: amd64, ext: "" }
          - { os: linux,   arch: arm64, ext: "" }
          - { os: windows, arch: amd64, ext: ".exe" }
          - { os: windows, arch: arm64, ext: ".exe" }
          - { os: darwin,  arch: amd64, ext: "" }
          - { os: darwin,  arch: arm64, ext: "" }
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod

      - name: Build (static)
        env:
          GOOS: ${{ matrix.os }}
          GOARCH: ${{ matrix.arch }}
          CGO_ENABLED: "0"
        run: |
          version="nightly-$(date -u +%Y%m%d)-${GITHUB_SHA::7}"
          out="bin/tcp-proxy-${GOOS}-${GOARCH}${{ matrix.ext }}"
          mkdir -p bin
          go build -trimpath -ldflags="-s -w -X main.version=${version}" -o "$out" ./cmd/tcp-proxy
          ls -la bin

      - uses: actions/upload-artifact@v7
        with:
          name: tcp-proxy-${{ matrix.os }}-${{ matrix.arch }}
          path: bin/*
          if-no-files-found: error

  nightly:
    name: Publish nightly release
    # 只在默认分支的 push 上发布，PR / 其他分支只构建不发布
    if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    concurrency:
      group: nightly-release
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v7

      - uses: actions/download-artifact@v8
        with:
          pattern: tcp-proxy-*
          path: dist
          merge-multiple: true

      - name: Generate checksums
        working-directory: dist
        run: |
          sha256sum tcp-proxy-* > SHA256SUMS.txt
          cat SHA256SUMS.txt

      - name: Delete old nightly release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release delete nightly --yes --cleanup-tag || true

      - name: Publish nightly release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create nightly dist/* \
            --title "Nightly Build" \
            --notes "auto build · ${GITHUB_SHA:0:7} · ${GITHUB_REF_NAME} · $(date -u +'%Y-%m-%d %H:%M UTC')" \
            --prerelease \
            --target "$GITHUB_SHA"
